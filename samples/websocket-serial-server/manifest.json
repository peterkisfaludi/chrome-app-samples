{
  "name": "WebSocket Serial Server",
  "description": "Runs a WebSocket server and relays Serial port messages to connected clients",
  "version": "1.0",
  "manifest_version": 2,

  "permissions": [
    "serial",
    {"socket": [
      "tcp-connect",
      "tcp-listen"]}],

  "app": {
    "background": {
      "scripts": ["background.js"]
    }
  }
}
